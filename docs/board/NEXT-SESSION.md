# Next session — founder notes

**Paused:** 2026-08-31 — Phase 3 comments expanded (recs + edit own + 3 photos + Lumen gate). Needs SQL **018** then **019** (and **017** if not pasted).

## Where we are

Dump/edit/photos frozen. Phase 3 UI: like on recs and days, comments on both, edit own note, up to 3 photos, byline. Lumen refuses hotel / PG-13 copy on notes.

**You:** paste in Supabase SQL Editor, in order if not already:
1. `017_lumen_city_hero_lock.sql`
2. `018_social.sql`
3. `019_comments.sql`

## First up (when that SQL is in)

Feel-pass like + a comment on a Zurich rec (edit it, add a pic). Then freeze this cut. Follow / profiles / Stripe still out.

## Dev

`cd apps/web && npm run dev` → http://localhost:3000  
`cd apps/web && npm run test:e2e`
